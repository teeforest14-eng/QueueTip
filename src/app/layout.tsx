import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { AppProviders } from "@/components/providers/app-providers";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "QueueTip — Family immigration guidance",
    template: "%s · QueueTip",
  },
  description:
    "Calm, structured help for family-based immigration: prepare filings, track cases, resolve issues, and find official tools—without legal overclaiming.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-qt-mist text-qt-text">
        <AppProviders>{children}</AppProviders>
      </body>
    </html>
  );
}
